# 安全策略

## 受支持版本

只有最新版本接收安全修复。skillferry 目前处于 alpha（0.x）；请据此对待。

## 报告漏洞

疑似漏洞不要开公开 issue。请私下联系维护者（GitHub 主页 / PyPI 元数据上
显示的所有者联系方式），并提供：

- 受影响的版本与平台，
- 最小复现（只用假 home——绝不用真实凭据），
- 观察到的影响，以及（如可能）建议的修复。

你将在 7 天内收到确认，30 天内收到状态更新。修复发布后，经报告者同意可
对报告署名致谢。

## 我们视为什么是漏洞

- 任何解析后的秘密值可能出现在 `plan --json`、`doctor --json`、日志、
  可共享导出或本地状态账本中的路径。
- 任何 `apply` 静默覆盖它不拥有的内容的路径。
- 任何 schema 允许字面秘密进入 workspace 定义的路径。
- 任何 workspace 可以逃出受管根目录（软链接、路径穿越）或管理
  `[protect]` 声明状态的路径。

## 什么不是漏洞

- 恶意 *workspace* 把 MCP 指向任意本地命令：workspace 是用户自己编写的
  输入，如同 shell 脚本；`plan` 预览与可移植性等级是审查面（见
  [THREAT_MODEL](docs/zh-CN/THREAT_MODEL.md)）。
- 拥有用户账户的本地攻击者：skillferry 不是秘密管理器，不会对静态存储的
  秘密再加密。
- Agent 侧行为（Agent 是否真的加载渲染出的文件）是产品兼容性问题，不是
  skillferry 漏洞。

## 披露与修复

修复以补丁版本发布，附带 CHANGELOG 条目、回归测试，以及——凡是触及秘密
的——更新后的威胁模型章节。
